#ifndef SDDS_UTILS_H__
#define SDDS_UTILS_H__

namespace sdds {
    // Foolproof for integer entry within limits
    int inputIntRange(int, int);

    // Finds length of a number's characters
    int numLen(int n);
}
#endif
